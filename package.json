{
  "name": "heroes-dojo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "compile": "tsc",
    "start": "yarn compile && node build",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@types/chance": "^1.1.3",
    "@types/jest": "^27.5.1",
    "@types/node": "^17.0.33",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.2",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "chance": "^1.1.8"
  },
  "jest": {
    "preset": "ts-jest",
    "testPathIgnorePatterns": ["mocks"]
  }
}
